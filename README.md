My Theme

A minimal starter WordPress theme.

Installation:
1. Copy this folder into `wp-content/themes/` of your WordPress install.
2. Activate the theme in the WP admin.

Notes:
- Edit `style.css` to change theme header information.
- Add translations to the `languages/` folder.
- Place additional template parts in `template-parts/`.
